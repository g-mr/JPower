spring:
  #redis配置
  redis:
    ##redis 单机环境配置
    host: ${jpower.dev.redis.host:}
    port: ${jpower.dev.redis.port:}
    password: ${jpower.dev.redis.password:}
    database: ${jpower.dev.redis.database:}
    ssl: false
    ##redis 集群环境配置
    #cluster:
    #  nodes: 127.0.0.1:6379,127.0.0.1:6378,127.0.0.1:6377
    #  commandTimeout: 5000
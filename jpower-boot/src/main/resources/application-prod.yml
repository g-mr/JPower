spring:
  #redis配置
  redis:
    ##redis 单机环境配置
    host: ${jpower.prod.redis.host:}
    port: ${jpower.prod.redis.port:}
    password: ${jpower.prod.redis.password:}
    database: ${jpower.prod.redis.database:}
    ssl: false
    ##redis 集群环境配置
    #cluster:
    #  nodes: 127.0.0.1:6379,127.0.0.1:6378,127.0.0.1:6377
    #  commandTimeout: 5000
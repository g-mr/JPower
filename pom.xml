<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>top.jpower</groupId>
    <artifactId>jpower</artifactId>
    <version>3.0.2</version>
    <packaging>pom</packaging>

    <name>JPower</name>
    <description>
        JPower微服务快速开发平台
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <top.jpower.version>3.0.2</top.jpower.version>

        <maven-compiler-plugin.version>3.14.1</maven-compiler-plugin.version>
        <maven-resources.version>3.4.0</maven-resources.version>
        <jib-maven-plugin.version>3.5.1</jib-maven-plugin.version>
        <spring-boot.version>3.5.8</spring-boot.version>
        <mybatis-flex.version>1.11.5</mybatis-flex.version>
        <auto.service.version>1.1.1</auto.service.version>

        <easy-captcha.version>1.6.2</easy-captcha.version>
        <spring-boot-admin.version>3.5.6</spring-boot-admin.version>
        <fastdfs.version>1.27.2</fastdfs.version>
        <aliyun.sms.version>4.3.1</aliyun.sms.version>
        <aliyun.oss.version>3.18.3</aliyun.oss.version>
        <qiniu.oss.version>7.19.0</qiniu.oss.version>
        <amazon.s3.version>2.29.32</amazon.s3.version>
        <deploy.version>3.1.2</deploy.version>

        <docker.image.prefix>jpower</docker.image.prefix>
        <docker.registry>docker.qidiangk.com</docker.registry>
    </properties>

    <modules>
        <module>jpower-auth</module>
        <module>jpower-core</module>
        <module>jpower-gateway</module>
        <module>jpower-api</module>
        <module>jpower-upms</module>
        <module>jpower-ops</module>
        <module>jpower-common</module>
        <module>jpower-boot</module>
    </modules>

    <dependencies>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>top.jpower</groupId>
                <artifactId>jpower-core-dependencies</artifactId>
                <version>${top.jpower.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <configuration>
                        <classifier>exec</classifier>
                        <!--生成可执行jar的名称-->
                        <finalName>${project.build.finalName}</finalName>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!-- docker -->
                <plugin>
                    <groupId>com.google.cloud.tools</groupId>
                    <artifactId>jib-maven-plugin</artifactId>
                    <version>${jib-maven-plugin.version}</version>
                    <configuration>
                        <from>
                            <image>registry.cn-hangzhou.aliyuncs.com/dockerhub_mirror/java:17-anolis</image>
                        </from>
                        <to>
                            <image>${docker.registry}/${docker.image.prefix}/${project.build.finalName}</image>
                            <tags>
                                <tag>${project.version}</tag>
                            </tags>
                        </to>
                        <container>
                            <environment>
                                <TZ>Asia/Shanghai</TZ>
                                <!--记得换成 skywalking 容器地址-->
                                <SW_AGENT_COLLECTOR_BACKEND_SERVICES>127.0.0.1:11800</SW_AGENT_COLLECTOR_BACKEND_SERVICES>
                                <SW_AGENT_NAME>${project.build.finalName}</SW_AGENT_NAME>
                            </environment>
                            <args>
                                <arg>--spring.profiles.active=test</arg>
                            </args>
                            <creationTime>USE_CURRENT_TIMESTAMP</creationTime>
                        </container>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources.version}</version>
                </plugin>

            </plugins>
        </pluginManagement>

        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>${deploy.version}</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <release>${java.version}</release>
                    <encoding>${project.build.sourceEncoding}</encoding>
					<compilerArgs>
						<arg>-parameters</arg>
					</compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.42</version>
                        </path>
                        <path>
                            <groupId>com.mybatis-flex</groupId>
                            <artifactId>mybatis-flex-processor</artifactId>
                            <version>${mybatis-flex.version}</version>
                        </path>
                        <path>
                            <groupId>com.google.auto.service</groupId>
                            <artifactId>auto-service</artifactId>
                            <version>${auto.service.version}</version>
                        </path>
                        <!-- 生成 META-INF/spring-configuration-metadata.json，供 IDE 进行配置属性提示与校验 -->
                        <path>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                            <version>${spring-boot.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <debug>false</debug>
                </configuration>
            </plugin>

        </plugins>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>
    </build>

    <!-- 指定 Maven 源 -->
    <repositories>
        <repository>
            <id>aliyunmaven</id>
            <name>aliyun</name>
            <url>https://maven.aliyun.com/repository/public</url>
        </repository>

        <repository>
            <id>huaweicloud</id>
            <name>huawei</name>
            <url>https://mirrors.huaweicloud.com/repository/maven/</url>
        </repository>

        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>

</project>
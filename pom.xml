<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>top.jpower</groupId>
    <artifactId>jpower</artifactId>
    <version>2.2.0</version>
    <packaging>pom</packaging>

    <name>JPower</name>
    <description>
        Java微服务快速开发平台
    </description>
    <url>https://gitee.com/gdzWork/JPower</url>

    <developers>
        <developer>
            <id>mr.g</id>
            <name>Tzu-Ding Goo</name>
            <email>ding931226@yeah.net</email>
            <organization>JPower</organization>
            <organizationUrl>https://gitee.com/gdzWork</organizationUrl>
            <timezone>+8</timezone>
        </developer>
    </developers>
    <licenses>
        <license>
            <name>The Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>
    <scm>
        <connection>https://gitee.com/gdzWork/JPower.git</connection>
        <developerConnection>https://gitee.com/gdzWork</developerConnection>
        <url>https://gitee.com/gdzWork/JPower.git</url>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>1.8</java.version>
        <com.jpower.version>2.2.0</com.jpower.version>

        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <easy-captcha.version>1.6.2</easy-captcha.version>
        <druid-spring.version>1.2.20</druid-spring.version>
        <fastjson.version>2.0.41</fastjson.version>
        <pagehelper-spring.version>1.4.1</pagehelper-spring.version>
        <javax.mail.version>1.6.2</javax.mail.version>
        <jwt.version>0.6.0</jwt.version>
        <redisson.version>3.17.7</redisson.version>
        <fastdfs.version>1.27.2</fastdfs.version>
        <aliyun.sms.version>2.0.24</aliyun.sms.version>
        <aliyun.oss.version>3.17.4</aliyun.oss.version>
        <qiniu.oss.version>7.15.0</qiniu.oss.version>
        <httpclient.version>4.5.13</httpclient.version>
        <guava.version>32.1.3-jre</guava.version>
        <poi.version>3.17</poi.version>
        <knife4j.version>4.4.0</knife4j.version>
        <swagger-annotations.version>1.6.2</swagger-annotations.version>
        <common-io.version>2.8.0</common-io.version>
        <skywalking.version>9.1.0</skywalking.version>
        <spring-boot-admin.version>2.7.15</spring-boot-admin.version>
        <logstash.version>7.0.1</logstash.version>
        <hutool.version>5.8.29</hutool.version>
        <spring-alibaba.version>2021.0.5.0</spring-alibaba.version>
        <spring-boot.version>2.7.18</spring-boot.version>
        <spring-cloud.version>2021.0.8</spring-cloud.version>

        <maven-compiler-plugin.version>3.8.1</maven-compiler-plugin.version>
        <maven-resources.version>3.3.1</maven-resources.version>

        <dockerfile-maven-plugin.version>1.4.13</dockerfile-maven-plugin.version>
        <docker.image.prefix>jpower</docker.image.prefix>
        <docker.registry>118.31.115.232:5000</docker.registry>

    </properties>

    <modules>
        <module>jpower-auth</module>
        <module>jpower-core</module>
        <module>jpower-gateway</module>
        <module>jpower-api</module>
        <module>jpower-upms</module>
        <module>jpower-ops</module>
        <module>jpower-common</module>
        <module>jpower-boot</module>
    </modules>

    <dependencies>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

    </dependencies>

    <dependencyManagement>
        <dependencies>

            <!-- spring boot 依赖 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- spring cloud 依赖 -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- spring cloud alibaba 依赖 -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- knife4j -->
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-dependencies</artifactId>
                <version>${knife4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 排除knife4j中的hutool -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <!-- spring-boot-admin -->
            <dependency>
                <groupId>de.codecentric</groupId>
                <artifactId>spring-boot-admin-starter-server</artifactId>
                <version>${spring-boot-admin.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <profiles>
        <profile>
            <id>release</id>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                    </plugin>
                    <!-- GPG -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <gpgArguments>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                        </configuration>
                    </plugin>

                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.9.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>jpower-central</publishingServerId>
                            <autoPublish>true</autoPublish>
                            <waitUntil>published</waitUntil>
                            <excludeArtifacts>
                                <excludeArtifact>jpower-api</excludeArtifact>
                                <excludeArtifact>jpower-auth</excludeArtifact>
                                <excludeArtifact>jpower-boot</excludeArtifact>
                                <excludeArtifact>jpower-common</excludeArtifact>
                                <excludeArtifact>jpower-gateway</excludeArtifact>
                                <excludeArtifact>jpower-ops</excludeArtifact>
                                <excludeArtifact>jpower-upms</excludeArtifact>
                                <excludeArtifact>jpower-admin</excludeArtifact>
                                <excludeArtifact>jpower-doc</excludeArtifact>
                                <excludeArtifact>jpower-log</excludeArtifact>
                                <excludeArtifact>jpower-resource-api</excludeArtifact>
                                <excludeArtifact>jpower-system-api</excludeArtifact>
                                <excludeArtifact>jpower-user-api</excludeArtifact>
                                <excludeArtifact>jpower-resource</excludeArtifact>
                                <excludeArtifact>jpower-system</excludeArtifact>
                                <excludeArtifact>jpower-user</excludeArtifact>
                            </excludeArtifacts>
                        </configuration>
                    </plugin>
                </plugins>
            </build>

        </profile>
    </profiles>

    <build>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <configuration>
                        <classifier>exec</classifier>
                        <fork>true</fork>
                        <!--生成可执行jar的名称-->
                        <finalName>${project.build.finalName}</finalName>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!-- war包解析 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <version>3.0.0</version>
                    <configuration>
                        <failOnMissingWebXml>false</failOnMissingWebXml>
                        <warName>${project.build.finalName}</warName>
                        <webResources>
                            <resource>
                                <directory>${maven.multiModuleProjectDirectory}/config</directory>
                                <targetPath>WEB-INF/classes</targetPath>
                                <filtering>false</filtering>
                                <includes>
                                    <include>bootstrap-jpower.yml</include>
                                </includes>
                            </resource>
                        </webResources>
                    </configuration>
                </plugin>
                <!-- dockerfile -->
                <plugin>
                    <groupId>com.spotify</groupId>
                    <artifactId>dockerfile-maven-plugin</artifactId>
                    <version>${dockerfile-maven-plugin.version}</version>
                    <configuration>
                        <useMavenSettingsForAuth>true</useMavenSettingsForAuth>
                        <repository>${docker.registry}/${docker.image.prefix}/${project.build.finalName}</repository>
                        <tag>${project.version}</tag>
                        <buildArgs>
                            <JAR_FILE>target/${project.build.finalName}-exec.jar</JAR_FILE>
                        </buildArgs>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.spotify</groupId>
                <artifactId>dockerfile-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>

        </plugins>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>
    </build>

</project>
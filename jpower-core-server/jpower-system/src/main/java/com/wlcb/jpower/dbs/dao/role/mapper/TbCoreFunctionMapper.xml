<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wlcb.jpower.dbs.dao.role.mapper.TbCoreFunctionMapper">


    <select id="listFunction"
            resultType="com.wlcb.jpower.vo.FunctionVo">
        select *,
        (
            SELECT CASE WHEN count( 1 ) > 0 THEN 1 ELSE 0 END
            FROM tb_core_function
            WHERE parent_id = f.id
            <if test="functionType!=null">
                AND function_type = ${functionType}
            </if>
        ) AS "has_children"
        from tb_core_function f
        ${ew.customSqlSegment}
    </select>

    <select id="listDataFunction"
            resultType="com.wlcb.jpower.vo.DataFunctionVo">
        select *,
               (
                   SELECT CASE WHEN count( 1 ) > 0 THEN 1 ELSE 0 END
                   FROM tb_core_function
                   WHERE parent_id = f.id AND function_type = 1
               ) AS "has_children",
               (
                   SELECT CASE WHEN count( 1 ) > 0 THEN 1 ELSE 0 END
                   FROM tb_core_function
                   WHERE parent_id = f.id AND function_type <![CDATA[ <> ]]> 1
               ) AS isData
        from tb_core_function f
            ${ew.customSqlSegment}
    </select>

</mapper>
spring:
  main:
    ## @FeignClient(value = "服务名") 设置可以有多个类存在相同的FeignClient 中的value值
    allow-bean-definition-overriding: true
  config:
    import:
      - "optional:classpath:./bootstrap.yml"
      - "optional:file:./conf/bootstrap.yml"